# Commit0071 — Default WS + CSA 2 Show

- Log Viewer starts in **2 logs (Dual)** mode.
- Only Pane 1 and Pane 2 are visible initially.
- Pane 1 default source is **WS**.
- Pane 2 default source is **CSA**.
- The existing explicit **LOAD LOGS** workflow is preserved.
- WS default Quick Filter remains **Level Err/Error + Message contains code and True**.
- Row bookmarks remain available from the row context menu.
