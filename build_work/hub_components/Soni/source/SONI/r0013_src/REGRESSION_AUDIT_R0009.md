# RC1 to RC2 Regression Audit

The regression began when ReplayContext/Snapshot/Coordinator became a mandatory gate for visible rendering. State could advance while the central overlay remained on a Planning CT/MR image. R0009 restores one synchronous transaction: decode frame, render central image, track thumbnails, update temperature cursor, render spectrum, update acoustic cards and labels. ReplayContext is now a non-blocking mirror.
